import {ObjectId } from 'mongodb';
import bcrypt from 'bcrypt';
import {gyms} from '../config/mongoCollections.js';

// Create gym
export const createGym = async (name, password, address, hours, equipment, classes, personal_trainers, extra) => {

}

// Get gym by id
export const getGymById = async (gymID) => {
    
}

// Update gym content
export const updateGym = async (gymID) => {
    
}

// Delete gym
export const deleteGym = async (gymID) => {
    
}